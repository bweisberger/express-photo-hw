<!DOCTYPE html>
<html lang="en">
    <head>
        <title>User Show Page</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link href="css/style.css" rel="stylesheet">
    </head>
    <body>
    <h1 class="user-heading"><%=user.name%>'s Photos</h1>
    <form action="/photos/new" method="get">
        <input type=submit value="Post New Photo"><br/>
    </form>
      <%for(let i = 0; i < photos.length; i++){%>
      <a href="/photos/<%=photos[i]._id%>"><h3 class="user-photo-title"><%=photos[i].title%></h3>
      <img src="<%=photos[i].url%>"></a>
      <%}%>
    </body>
</html>